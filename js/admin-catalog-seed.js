import './admin-apple.js';
import './admin-apple-seed.js';
import './admin-code-manager.js';
import { getApps, getApp } from 'https://www.gstatic.com/firebasejs/10.13.2/firebase-app.js';
import { getAuth, onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/10.13.2/firebase-auth.js';
import { getFirestore, collection, getDocs, doc, setDoc, serverTimestamp } from 'https://www.gstatic.com/firebasejs/10.13.2/firebase-firestore.js';
const app=getApps().length?getApp():null;if(!app)throw new Error('Firebase app is not initialized.');const auth=getAuth(app),db=getFirestore(app);const starterApps=[{id:'farmakon',name:'Farmakon',category:'Pharmacy',description:'A pharmacology tool for students and healthcare learners.',platforms:['Android','iPhone']},{id:'mcqstar',name:'MCQStar',category:'Study Tools',description:'Question practice and study review app.',platforms:['Android','iPhone']},{id:'q2mid',name:'Q2Mid',category:'Study Tools',description:'A focused study and revision assistant.',platforms:['Android','Tablet']},{id:'hepatix',name:'Hepatix',category:'Medicine',description:'A clinical medicine learning tool.',platforms:['iPhone','iPad']},{id:'medi3y',name:'Medi3y',category:'Medicine',description:'Medical tools and educational content.',platforms:['Android','iPhone','iPad']}];async function seedMissingApps(){const s=await getDocs(collection(db,'apps')),existing=new Set(s.docs.map(d=>d.id)),missing=starterApps.filter(a=>!existing.has(a.id));await Promise.all(missing.map(a=>setDoc(doc(db,'apps',a.id),{name:a.name,category:a.category,version:'',description:a.description,iconUrl:'',platforms:a.platforms,telegramUrls:{},active:true,updatedAt:serverTimestamp()})));return missing.length}function addSeedButton(){const h=document.querySelector('#apps .section-head');if(!h||document.getElementById('seedCatalogBtn'))return;const b=document.createElement('button');b.id='seedCatalogBtn';b.className='btn ghost';b.innerHTML='<i class="fa-solid fa-wand-magic-sparkles"></i> Initialize starter catalog';h.querySelector('#newAppBtn')?.before(b);b.onclick=async()=>{b.disabled=true;try{const n=await seedMissingApps();alert(n?`${n} starter applications are now real editable records.`:'The starter catalog is already editable.');location.reload()}catch(e){alert(e?.message||'Unable to initialize catalog.');b.disabled=false}}}document.addEventListener('DOMContentLoaded',addSeedButton);onAuthStateChanged(auth,async u=>{if(!u)return;try{const s=await getDocs(collection(db,'apps'));if(!s.size)addSeedButton()}catch(e){console.warn('Catalog check failed',e)}});