import{initializeApp}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-app.js';import{getAuth}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-auth.js';import{getFirestore,getDocs,collection}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-firestore.js';import{getFunctions,httpsCallable}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-functions.js';const c={apiKey:'AIzaSyATRvlq7VzIYFrVSprw5yVzv0uu5d-QrVM',authDomain:'medsbox-pro.firebaseapp.com',projectId:'medsbox-pro',storageBucket:'medsbox-pro.firebasestorage.app',messagingSenderId:'145094360411',appId:'1:145094360411:web:c8a525881927c294682e7e'},a=initializeApp(c),auth=getAuth(a),db=getFirestore(a),createCode=httpsCallable(getFunctions(a),'createActivationCode');const b=document.getElementById('newCodeBtn');if(b)b.onclick=async()=>{const s=await getDocs(collection(db,'plans')),plans=s.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.active!==false);const options=plans.map(x=>`<option value="${x.id}" data-days="${x.durationDays??''}">${x.name||x.id} — ${x.price??''} ${x.currency||'USD'}</option>`).join('');const overlay=document.getElementById('formOverlay'),content=document.getElementById('formContent');content.innerHTML=`<p class=eyebrow>ACTIVATION</p><h2>إنشاء كود تفعيل</h2><form id=dynamicCode class=admin-form><label>الخطة<select name=plan>${options}</select></label><label>المدة بالأيام<input name=days type=number min=1 value="365"></label><label>ملاحظة<input name=note placeholder="مثلاً: طلب Telegram"></label><button class="btn primary full">إنشاء الكود</button></form><div id=generatedCode></div>`;overlay.classList.add('open');document.getElementById('dynamicCode').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{const r=await createCode({planId:f.get('plan'),durationDays:Number(f.get('days')),note:f.get('note')});document.getElementById('generatedCode').innerHTML=`<div class=code-result>${r.data.code}</div>`}catch{alert('تعذر إنشاء الكود')}}};
