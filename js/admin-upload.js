import{initializeApp}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-app.js';import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-auth.js';import{getFirestore,doc,setDoc,getDoc,serverTimestamp}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-firestore.js';import{getStorage,ref,uploadBytes}from'https://www.gstatic.com/firebasejs/10.13.2/firebase-storage.js';const cfg={apiKey:'AIzaSyATRvlq7VzIYFrVSprw5yVzv0uu5d-QrVM',authDomain:'medsbox-pro.firebaseapp.com',projectId:'medsbox-pro',storageBucket:'medsbox-pro.firebasestorage.app',messagingSenderId:'145094360411',appId:'1:145094360411:web:c8a525881927c294682e7e'},app=initializeApp(cfg),auth=getAuth(app),db=getFirestore(app),storage=getStorage(app);const btn=document.createElement('button');btn.className='btn primary';btn.textContent='رفع ملف تطبيق';btn.type='button';let ready=false;function ui(){const overlay=document.createElement('div');overlay.className='modal-overlay';overlay.id='uploadOverlay';overlay.innerHTML='<div class="modal glass-card"><button class="close" id="uploadClose">×</button><p class="kicker">APP ASSETS</p><h2>رفع ملف التطبيق</h2><p>اربط ملف APK أو IPA أو AAB أو ZIP بتطبيق موجود أو جديد.</p><form id="uploadForm" class="admin-form"><label>معرف التطبيق<input name="id" required placeholder="farmakon"></label><label>المنصة<select name="platform"><option>Android</option><option>iPhone</option><option>iPad</option><option>Tablet</option></select></label><label>الملف<input name="file" type="file" accept=".apk,.ipa,.aab,.zip" required></label><button class="btn primary full" type="submit">رفع وحفظ</button></form><p id="uploadStatus" class="message"></p></div>';document.body.appendChild(overlay);const form=overlay.querySelector('#uploadForm'),status=overlay.querySelector('#uploadStatus');overlay.querySelector('#uploadClose').onclick=()=>overlay.remove();overlay.onclick=e=>{if(e.target===overlay)overlay.remove()};form.onsubmit=async e=>{e.preventDefault();const f=new FormData(form),id=String(f.get('id')).trim().toLowerCase().replace(/[^a-z0-9_-]/g,''),platform=String(f.get('platform')),file=f.get('file');if(!id||!file){status.textContent='أكمل البيانات أولاً.';return}status.textContent='جارٍ رفع الملف…';const path=`app-assets/${id}/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;try{const existing=await getDoc(doc(db,'apps',id));const data=existing.exists()?existing.data():{};const storagePaths={...(data.storagePaths||{}),[platform]:path};const platforms=[...(data.platforms||[]),platform].filter((x,i,a)=>a.indexOf(x)===i);await uploadBytes(ref(storage,path),file);await setDoc(doc(db,'apps',id),{storagePaths,platforms,active:true,updatedAt:serverTimestamp()},{merge:true});status.textContent='تم الرفع وربط الملف بالتطبيق ✓';setTimeout(()=>location.reload(),700)}catch(err){status.textContent='فشل الرفع: '+(err?.message||'خطأ غير معروف')}}};return overlay}btn.onclick=()=>{if(ready)ui()};onAuthStateChanged(auth,async u=>{if(!u)return;try{const a=await getDoc(doc(db,'admins',u.uid));ready=a.exists()&&a.data().enabled===true;if(ready){const target=document.getElementById('newAppBtn');if(target)target.parentElement.appendChild(btn)}}catch{}});
