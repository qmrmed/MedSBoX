const PLANS={yearly:{id:'yearly',name:'Annual access',price:10,period:'per year',description:'One year of access to 100+ ready-to-use Android applications.'},lifetime:{id:'lifetime',name:'Lifetime access',price:25,period:'one time',description:'One-time access to 100+ ready-to-use Android applications.'}};
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
let currentPlan=new URLSearchParams(location.search).get('plan')==='lifetime'?'lifetime':'yearly';
const refCode=`MSB-${crypto.getRandomValues(new Uint32Array(1))[0].toString(36).toUpperCase().slice(0,8)}`;
$('refCode').textContent=refCode;$('refCode').title='Copy reference';
$('refCode').onclick=async()=>{try{await navigator.clipboard.writeText(refCode);$('refCode').title='Reference copied'}catch{}};
const selected=()=>PLANS[currentPlan]||PLANS.yearly;
function updateTelegram(){const p=selected(),msg=['Hello, I would like to subscribe and activate MedSBoX Pro.',`Plan: ${p.name} — $${p.price} ${p.period}`,`Reference: ${refCode}`,'Access: 100+ Android applications'];$('telegramCta').href=`https://t.me/ID29i?text=${encodeURIComponent(msg.join('\n'))}`}
function render(){const p=selected();$('planToggle').innerHTML=Object.values(PLANS).map(x=>`<button type="button" class="plan-toggle-btn ${x.id===p.id?'active':''}" data-plan="${x.id}" aria-pressed="${x.id===p.id?'true':'false'}"><span class="pt-name">${esc(x.name)}</span><span class="pt-price"><bdi>$${x.price}</bdi><small> / ${esc(x.period)}</small></span></button>`).join('');document.querySelectorAll('.plan-toggle-btn').forEach(b=>b.addEventListener('click',()=>{currentPlan=b.dataset.plan;render()}));$('planStatus').textContent=`Selected plan: ${p.name} — $${p.price} ${p.period}`;$('planStatus').className='plan-status';updateTelegram()}
$('telegramCta').addEventListener('click',()=>{$('planStatus').textContent=`Opening Telegram for ${selected().name} activation…`});
render();