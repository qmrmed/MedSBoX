const PLANS={yearly:{id:'yearly',name:'Annual access',price:10,period:'per year',description:'One year of access to the complete MedSBoX Android library.'},lifetime:{id:'lifetime',name:'Lifetime access',price:25,period:'one time',description:'One-time access to the complete MedSBoX Android library.'}};const $=id=>document.getElementById(id),params=new URLSearchParams(location.search),requested=params.get('plan'),refCode=`MSB-${crypto.getRandomValues(new Uint32Array(1))[0].toString(36).toUpperCase().slice(0,8)}`,state={plan:PLANS[requested]||PLANS.yearly};$('refCode').textContent=refCode;$('refCode').onclick=async()=>{try{await navigator.clipboard.writeText(refCode);$('refCode').title='Copied'}catch{}};function render(){const p=state.plan;$('planToggle').innerHTML=Object.values(PLANS).map(x=>`<button type="button" class="plan-toggle-btn ${x.id===p.id?'active':''}" data-plan="${x.id}" aria-pressed="${x.id===p.id?'true':'false'}"><span class="pt-name">${x.name}</span><span class="pt-price"><bdi>$${x.price}</bdi><small>${x.period}</small></span></button>`).join('');document.querySelectorAll('.plan-toggle-btn').forEach(b=>b.onclick=()=>{state.plan=PLANS[b.dataset.plan]||PLANS.yearly;render()});$('planStatus').textContent=`${p.name} — $${p.price} ${p.period}`;const msg=['Hello, I want to subscribe and activate MedSBoX Pro.',`Plan: ${p.name} — $${p.price} ${p.period}`,`Reference: ${refCode}`,'Access: 100+ Android applications'];$('telegramCta').href=`https://t.me/ID29i?text=${encodeURIComponent(msg.join('\n'))}`}render();